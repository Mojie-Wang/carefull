# Careful Web

基于 `Vite + Vue 3 + Less` 的响应式网页基础项目，默认适配 `PC / Pad / Mobile`。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 目录

- `src/App.vue`: 当前首页骨架
- `src/styles/main.less`: 全局 Less 样式
- `src/components`: 组件目录
- `public/images`: 静态图片目录
